# pnpm 10.16+ enforces a default 24h `minimumReleaseAge` supply-chain policy.
# Our own first-party SDK is published by us and consumed immediately after
# (the release and the frontend bump are one change), so it is exempt while
# every third-party package keeps the delay.
minimum-release-age-exclude[]=@actos-dev/actos
